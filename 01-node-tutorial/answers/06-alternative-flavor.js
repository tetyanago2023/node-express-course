// 06-alternative-flavor.js

// Create an array and an object and export them
module.exports.food = ["Pizza", "Pasta"];

// Create an object `user` and export it
const user = {
    name: "Tetyana",
    }

module.exports.myPerson = user;
